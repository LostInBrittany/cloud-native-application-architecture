apiVersion: v1
kind: Pod
metadata:
  name: api-spy
  namespace: default
spec:
  serviceAccountName: monitor-sa  # UPDATED: Use our custom identity
  containers:
  - name: spy
    image: bitnami/kubectl:latest
    command: ["sleep", "3600"]
